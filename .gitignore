# Node (only needed if you want to re-run Capacitor CLI tooling; not needed to build in Xcode)
node_modules/

# Xcode build artifacts
ios/App/build/
ios/DerivedData/
DerivedData/
*.xcuserstate

# misc
**/.DS_Store
*.log
